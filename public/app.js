import {getState,recordResult,exportState,importState} from './db.js';
import {courseView,challengeFor,checkChallenge} from './learning.js';
const main=document.querySelector('#main'),nav=[...document.querySelectorAll('[data-route]')],guide=document.querySelector('#guide'),guideBody=document.querySelector('#guide-body');
let data={units:[],topics:[],lessons:[],masteryIds:[],activities:[],byUnit:{}},corpus='',policy=null,state=await getState();
const FALLBACK={authority:{normativeCeiling:'Canonical Shelf Statement of Faith',rule:'The Guide may explain positions beyond the Statement of Faith but may not establish them as Canonical Shelf doctrine.'},lgbtq:{claims:['LGBTQ people possess equal dignity and belonging.','Homosexual or bisexual orientation is not inherently sinful.','Faithful same-sex relationships and marriage may embody Christian virtue.','LGBTQ identity does not disqualify worship, service, teaching, leadership, or spiritual gifts.']},interpretiveRules:['Distinguish biblical text, historical context, lexical evidence, interpretation, reception history, doctrine, and application.','Do not render contested evidence as scholarly consensus.'],queerReception:{ruthNaomi:{allowed:'Some Christian and biblical interpreters read Ruth and Naomi through lesbian, homoerotic, female-same-sex-love, or queer-kinship lenses.',boundary:'The biblical narrator does not explicitly identify Ruth and Naomi as sexual partners; present this as reception history or interpretation, not uncontested textual fact.'}}};
async function load(){try{data=await fetch('/data/catalog.json').then(r=>r.ok?r.json():Promise.reject())}catch{}try{corpus=await fetch('/data/corpus.txt').then(r=>r.ok?r.text():'')}catch{}try{policy=await fetch('/data/theology-policy.json').then(r=>r.ok?r.json():FALLBACK)}catch{policy=FALLBACK}}await load();policy||=FALLBACK;
const esc=(s='')=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const route=()=>((location.hash.replace(/^#\//,'').split('?')[0]||'home').split('/')[0]),params=()=>new URLSearchParams(location.hash.split('?')[1]||'');
function setCurrent(r){nav.forEach(a=>a.toggleAttribute('aria-current',a.dataset.route===r))}function progress(){const done=state.completed?.filter(id=>id.startsWith('lesson:')||id.startsWith('mastery:')).length||0;return{done,pct:Math.round(done/139*100)}}
function shell(title,eye,body){return `<header class="section"><p class="eyebrow">${esc(eye)}</p><h1>${esc(title)}</h1></header>${body}`}
function nextUnit(){return data.units.find(u=>(data.byUnit?.[u.id]||[]).some(id=>!state.completed.includes(id)))}
function home(){const s=progress(),next=nextUnit();return `<section class="hero"><div><p class="eyebrow">Bible literacy for thoughtful adults</p><h1>Read with context.<br>Think with care.</h1><p class="lede">A self-paced course, complete Bible, curated reference library, and evidence-aware study guide. Progress measures understanding, not theological assent.</p><p><a class="button" href="${next?`#/course?unit=${encodeURIComponent(next.id)}`:'#/course'}">${next?'Continue the course':'Review the course'}</a></p></div><div><div class="stat"><strong>${s.pct}%</strong><span>${s.done}/139 activities complete</span></div><div class="stat"><strong>25</strong><span>integrated units</span></div><div class="stat"><strong>${data.topics.length||45}</strong><span>curated Topics</span></div></div></section><section class="section"><h2>Your study shelf</h2><div class="grid"><article class="card"><p class="eyebrow">Course</p><h3>${esc(next?.title||'Independent review')}</h3><p>${esc(next?.scope||'Return to any unit and strengthen recall.')}</p><a href="#/course">Open Course</a></article><article class="card"><p class="eyebrow">Bible</p><h3>Read the text itself</h3><p>Keep biblical text distinct from later interpretation.</p><a href="#/bible">Open Bible</a></article><article class="card"><p class="eyebrow">Topics</p><h3>Follow a question</h3><p>Doctrine, ethics, history, practice, and contested interpretations.</p><a href="#/topics">Browse Topics</a></article></div></section>`}
function bible(){const q=params().get('q')||'',lines=q&&corpus?corpus.split('\n').filter(x=>x.toLowerCase().includes(q.toLowerCase())).slice(0,120):[];return shell('Bible','Scripture reader',`<form class="search" id="bible-search"><label class="sr-only" for="bq">Search Bible text</label><input id="bq" name="bq" value="${esc(q)}" placeholder="Reference, word, or phrase"><button>Search</button></form>${!corpus?'<p class="notice">Run the migration build to populate the local Bible corpus.</p>':''}${q?`<section class="section reader"><h2>${lines.length} matching lines</h2><pre>${esc(lines.join('\n'))||'No match found.'}</pre></section>`:'<section class="section"><p class="lede">Search the complete local corpus. Scripture remains available independently of the Guide.</p></section>'}`)}
function topics(){const id=params().get('topic');if(id){const t=data.topics.find(x=>x.id===id);if(!t)return shell('Topic not found','Topics','');return `<article class="reader"><p><a href="#/topics">← All Topics</a></p><p class="eyebrow">Reference · not scored</p><h1>${esc(t.title)}</h1><p class="lede">${esc(t.answer||t.summary||'')}</p>${(t.body||t.sections||[]).map?.(x=>typeof x==='string'?`<p>${esc(x)}</p>`:'')?.join('')||''}<p><button class="button" data-ask="${esc(t.title)}">Ask the Guide about this</button></p></article>`}return shell('Topics','Curated reference · not scored',`<p class="lede">Direct answers, distinctions, Scripture connections, competing interpretations, and source trails.</p><div class="topic-list">${data.topics.map(t=>`<article class="topic-item"><h3><a href="#/topics?topic=${encodeURIComponent(t.id)}">${esc(t.title)}</a></h3><p>${esc(t.answer||t.summary||'').slice(0,180)}</p></article>`).join('')||'<p class="notice">Run migration to populate Topics.</p>'}</div>`)}
function practice(){const s=progress();return shell('Practice','Retrieval and transfer',`<p class="lede">Practice reinforces the course without becoming a second curriculum. Completion, mastery attempts, and review counts are stored separately.</p><div class="grid"><article class="card"><h3>Context vs. application</h3><p>Practice distinguishing observation, context, interpretation, doctrine, reception history, and application.</p></article><article class="card"><h3>Canonical orientation</h3><p>${data.masteryIds.length||69} stable mastery competencies.</p></article><article class="card"><h3>Your progress</h3><p>${s.done}/139 activities complete.</p><button class="button" id="export">Export progress</button> <button class="button" id="import">Import progress</button></article></div>`)}
function searchPage(q){const n=q.toLowerCase(),topics=data.topics.filter(t=>`${t.title} ${t.answer||''} ${(t.tags||[]).join(' ')}`.toLowerCase().includes(n)).slice(0,12),units=data.units.filter(u=>`${u.title} ${u.scope}`.toLowerCase().includes(n)),bible=corpus?corpus.split('\n').filter(x=>x.toLowerCase().includes(n)).slice(0,12):[];return shell(`Search: “${q}”`,'Across Canonical Shelf',`<div class="results"><section><h2>Bible</h2>${bible.map(x=>`<div class="result">${esc(x)}</div>`).join('')||'<p>No local text matches.</p>'}</section><section><h2>Topics</h2>${topics.map(t=>`<div class="result"><a href="#/topics?topic=${encodeURIComponent(t.id)}"><strong>${esc(t.title)}</strong></a><p>${esc(t.answer||'').slice(0,220)}</p></div>`).join('')||'<p>No Topic matches.</p>'}</section><section><h2>Course</h2>${units.map(u=>`<div class="result"><a href="#/course?unit=${encodeURIComponent(u.id)}"><strong>${esc(u.title)}</strong></a><p>${esc(u.scope)}</p></div>`).join('')||'<p>No course matches.</p>'}</section><button class="button" data-ask="${esc(q)}">Ask the Guide about this</button></div>`)}
function render(){const r=route();setCurrent(r);if(r==='search')main.innerHTML=searchPage(params().get('q')||'');else if(r==='course')main.innerHTML=data.units.length?courseView(data,state,params(),esc):shell('Course','Migration required','<p class="notice">Run bun run migrate.</p>');else main.innerHTML=({home,bible,topics,practice}[r]||home)();main.focus({preventScroll:true})}
function guideAnswer(q){const n=q.toLowerCase();let lead=`${policy.authority.rule}`;if(/gay|lesbian|homosexual|lgbt|queer|same[- ]sex/.test(n))lead=policy.lgbtq.claims.join(' ');if(/ruth|naomi/.test(n))lead=`${policy.queerReception.ruthNaomi.allowed} ${policy.queerReception.ruthNaomi.boundary}`;const relTopics=data.topics.filter(t=>`${t.title} ${t.answer||''}`.toLowerCase().split(/\s+/).some(w=>w.length>5&&n.includes(w))).slice(0,5),relUnits=data.units.filter(u=>`${u.title} ${u.scope}`.toLowerCase().split(/\s+/).some(w=>w.length>5&&n.includes(w))).slice(0,4);guideBody.innerHTML=`<form id="guide-form"><label for="guide-q">Ask a study question</label><textarea id="guide-q" name="question" rows="3">${esc(q)}</textarea><button class="button">Ask</button></form><div class="evidence"><p class="lede">${esc(lead)}</p><p>${esc(policy.interpretiveRules.join(' '))}</p></div>${relTopics.length?`<div class="evidence"><h3>Relevant Topics</h3>${relTopics.map(t=>`<p><strong>${esc(t.title)}</strong><br>${esc(t.answer||'').slice(0,260)}</p>`).join('')}</div>`:''}${relUnits.length?`<div class="evidence"><h3>Course connections</h3>${relUnits.map(u=>`<p>${u.sequence}. ${esc(u.title)}</p>`).join('')}</div>`:''}<div class="evidence"><span class="badge">offline evidence mode</span><span class="badge">bounded by ${esc(policy.authority.normativeCeiling)}</span></div>`;guide.hidden=false}
window.addEventListener('hashchange',render);
document.addEventListener('submit',async e=>{if(e.target.id==='global-search'){e.preventDefault();const q=new FormData(e.target).get('q').trim();if(q)location.hash=`#/search?q=${encodeURIComponent(q)}`}if(e.target.id==='bible-search'){e.preventDefault();location.hash=`#/bible?q=${encodeURIComponent(new FormData(e.target).get('bq')||'')}`}if(e.target.id==='guide-form'){e.preventDefault();guideAnswer(new FormData(e.target).get('question')||'')}if(e.target.matches('.challenge')){e.preventDefault();const id=e.target.dataset.activity,ch=challengeFor(data,id,Number(e.target.dataset.index)),ok=checkChallenge(e.target,ch);state=await recordResult(id,ok);const f=e.target.querySelector('.feedback');f.innerHTML=ok?`<p class="notice"><strong>Correct.</strong> ${esc(ch?.why||'Your response is supported by the activity.')}</p>`:`<p class="notice"><strong>Not yet.</strong> ${esc(ch?.hint||'Return to the evidence and try again.')}</p>`}});
document.addEventListener('click',async e=>{const a=e.target.closest('[data-ask]');if(a){guideAnswer(a.dataset.ask);guide.hidden=false}if(e.target.id==='export'){const blob=new Blob([await exportState()],{type:'application/json'}),x=document.createElement('a');x.href=URL.createObjectURL(blob);x.download='canonical-shelf-progress.json';x.click();URL.revokeObjectURL(x.href)}if(e.target.id==='import'){const input=document.createElement('input');input.type='file';input.accept='application/json';input.onchange=async()=>{try{await importState(await input.files[0].text());state=await getState();render()}catch(err){alert(err.message)}};input.click()}});
document.querySelector('#guide-open').addEventListener('click',()=>{guideAnswer('What can you help me study?');document.querySelector('#guide-q')?.focus()});document.querySelector('#guide-close').addEventListener('click',()=>{guide.hidden=true;document.querySelector('#guide-open').focus()});
if('serviceWorker'in navigator)navigator.serviceWorker.register('/sw.js').catch(()=>{});render();
